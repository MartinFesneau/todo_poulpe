function updateTask () {
  var task = document.querySelector(`#task-${"<%= @task.id %>"}`);
  task.innerHTML = ""
  task.innerHTML = "<%= j render "tasks/task", task: @task %>";
}

updateTask();
<%# call those global function to set up the event listeners on the edited task %>
expandCard();
showEditForm();