<div>
  <%= simple_form_for([task, comment], remote: true, html: { class: "form-inline ml-2"}) do |f| %>
    <%= f.input :content, class:"form-control-sm", label: false, placeholder: "Add a new comment" %>
    <%= f.submit "Submit", class: "btn btn-primary" %>
  <% end %>
</div>